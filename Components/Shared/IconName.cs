namespace _10xnotes.Components.Shared;

/// <summary>
/// Every icon the design uses. <see cref="Icon"/> renders each one as literal inline SVG, so a
/// name here is a compile-time guarantee that the markup for it exists.
/// </summary>
public enum IconName
{
    Logo,
    FileText,
    File,
    Layers,
    Plus,
    User,
    Logout,
    Login,
    UserPlus,
    Clock,
    ArrowRight,
    ArrowUpRight,
    ChevronRight,
    Clipboard,
    Upload,
    Sparkle,
    Check,
    Pencil,
    Eye,
    Trash,
    Mail,
    Lock,
    Alert,
    X,
}
